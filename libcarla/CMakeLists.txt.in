add_definitions(-DBOOST_ERROR_CODE_HEADER_ONLY)
add_definitions(-DLIBCARLA_TEST_CONTENT_FOLDER="")

set(BOOST_INCLUDE_PATH "${CONAN_INCLUDE_DIRS_BOOST}")
set(BOOST_LIB_PATH "${CONAN_LIB_DIRS_BOOST}")

set(RPCLIB_INCLUDE_PATH "${CONAN_INCLUDE_DIRS_RPCLIB}")
set(RPCLIB_LIB_PATH "${CONAN_LIB_DIRS_RPCLIB}")

set(GTEST_INCLUDE_PATH "${CONAN_INCLUDE_DIRS_GTEST}")
set(GTEST_LIB_PATH "${CONAN_LIB_DIRS_GTEST}")

set(RECAST_INCLUDE_PATH "${CONAN_INCLUDE_DIRS_RECASTLIB}")
set(RECAST_LIB_PATH "${CONAN_LIB_DIRS_RECASTLIB}")

set(LIBPNG_INCLUDE_PATH "${CONAN_INCLUDE_DIRS_LIBPNG}")
set(LIBPNG_LIB_PATH "${CONAN_LIB_DIRS_LIBPNG}")
